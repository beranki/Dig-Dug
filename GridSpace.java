import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class GridSpace here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class GridSpace extends Space
{
    /**
     * Act - do whatever the GridSpace wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */
    public GridSpace(Color color) {
        super(color);
    }
    public GridSpace() {
        super(new Color(254,180,0));
    }
}
